venv/
__pycache__/
*.pyc
*.backup
*.backup-*

# secrets — service account private keys etc. NEVER track
secrets/

# database files — never track; prod DB lives at /var/data/refunds/db.sqlite
db.sqlite
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
